# agent-ui 開発環境用Dockerfile
FROM node:18-alpine

# pnpmをインストール
RUN npm install -g pnpm

# 作業ディレクトリを設定
WORKDIR /app

# 依存関係ファイルをコピー
COPY package.json pnpm-lock.yaml* ./

# 依存関係をインストール
RUN pnpm install

# ポートを公開
EXPOSE 3000

# 開発サーバーを起動
CMD ["pnpm", "dev"]
